<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.srp.carwash.ui.login.LoginViewModel" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/_70sdp"
            android:gravity="center"
            android:layoutDirection="ltr"
            android:orientation="horizontal">

            <TextView
                splashFont="@{true}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:text="FOR"
                android:textColor="@color/colorAccent2"
                android:textSize="@dimen/_40ssp" />

            <TextView
                splashFont="@{true}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:text="CAST"
                android:textColor="@color/colorAccent"
                android:textSize="@dimen/_40ssp" />

        </LinearLayout>

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/_30sdp"
            android:layout_marginTop="@dimen/_50sdp"
            android:layout_marginRight="@dimen/_30sdp"
            android:layout_marginBottom="@dimen/_20sdp"
            app:cardBackgroundColor="@color/colorPrimaryDark2"
            app:cardCornerRadius="@dimen/_5sdp">


            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/_20sdp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:layout_marginTop="@dimen/_15sdp"
                    android:gravity="center"
                    android:text="@{viewModel.step ==0 ? @string/insert_mobile : @string/insert_code}"
                    android:textColor="@color/light" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:layout_marginTop="@dimen/_5sdp"
                    android:gravity="center"
                    android:text="@{viewModel.mobile}"
                    android:textColor="@color/light"
                    android:visibility="@{viewModel.step==1 ? View.VISIBLE : View.GONE}" />


                <com.srp.carwash.component.SmartEditText
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="left|center_horizontal"
                    android:layout_marginLeft="@dimen/_10sdp"
                    android:layout_marginTop="@dimen/_10sdp"
                    android:layout_marginRight="@dimen/_10sdp"
                    android:background="@color/edittextbg"
                    android:gravity="left|center_horizontal"
                    android:inputType="phone"
                    android:maxLength="11"
                    android:maxLines="1"
                    android:onTextChanged="@{viewModel::verifyMobile}"
                    android:padding="@dimen/_5sdp"
                    android:text="@={viewModel.mobile}"
                    android:textColor="@color/light"
                    android:textColorHint="@color/light"
                    android:visibility="@{viewModel.step==0 ? View.VISIBLE : View.GONE}" />

                <com.srp.carwash.component.SmartEditText
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="left|center_horizontal"
                    android:layout_marginLeft="@dimen/_10sdp"
                    android:layout_marginTop="@dimen/_10sdp"
                    android:layout_marginRight="@dimen/_10sdp"
                    android:background="@color/edittextbg"
                    android:gravity="left|center_horizontal"
                    android:inputType="phone"
                    android:maxLength="5"
                    android:maxLines="1"
                    android:onTextChanged="@{viewModel::verifyCode}"
                    android:padding="@dimen/_5sdp"
                    android:text="@={viewModel.code}"
                    android:textColor="@color/light"
                    android:textColorHint="@color/light"
                    android:visibility="@{viewModel.step==1 ? View.VISIBLE : View.GONE}" />


                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:layout_marginTop="@dimen/_4sdp"
                    android:gravity="center"
                    android:text="@{viewModel.step==0 ? @string/error_mobile : @string/error_code }"
                    android:textColor="@color/red"
                    android:textSize="@dimen/_10ssp"
                    android:visibility="@{viewModel.isError==0 ? View.INVISIBLE : View.VISIBLE}" />


                <androidx.cardview.widget.CardView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:layout_marginTop="@dimen/_30sdp"
                    android:layout_marginBottom="@dimen/_20sdp"
                    android:onClick="@{()-> viewModel.onClicked()}"
                    app:cardBackgroundColor="@color/colorAccent2"
                    app:cardCornerRadius="@dimen/_5sdp">

                    <RelativeLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="vertical">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_centerHorizontal="true"
                            android:layout_centerVertical="true"
                            android:paddingLeft="@dimen/_40sdp"
                            android:paddingTop="@dimen/_7sdp"
                            android:paddingRight="@dimen/_40sdp"
                            android:paddingBottom="@dimen/_7sdp"
                            android:text="@{viewModel.step==0 ? @string/login : @string/verify}"
                            android:textColor="@color/white"
                            android:visibility="@{viewModel.loadingStatus ? View.INVISIBLE : View.VISIBLE}" />


                        <ProgressBar
                            android:id="@+id/pbHeaderProgress"
                            style="?android:attr/progressBarStyleSmall"
                            android:layout_width="@dimen/_25sdp"
                            android:layout_height="@dimen/_25sdp"
                            android:layout_centerHorizontal="true"
                            android:layout_centerVertical="true"
                            android:layout_gravity="center"
                            android:theme="@style/ProgressBarTheme"
                            android:visibility="@{viewModel.loadingStatus ? View.VISIBLE : View.INVISIBLE}"></ProgressBar>

                    </RelativeLayout>


                </androidx.cardview.widget.CardView>


            </LinearLayout>


        </androidx.cardview.widget.CardView>
    </LinearLayout>
</layout>